# Tools

Expected tools:

- shell execution for `git`, `python3`, and OpenClaw cron runs
- file read/write in this workspace
- network access for GitHub fetches and Resend email API

The Python scripts use only the standard library.
